---
description: 团队核心编码原则 — 所有 Agent 会话默认生效
alwaysApply: true
---

# 核心原则

- 单次任务单一意图，PR 尽量 < 400 行
- 先读 AGENTS.md 和相关架构文档，再动手
- 最小 diff：只改任务范围内的文件
- 编码后必须运行 `./scripts/validate.sh` 并贴出结果
- 不确定架构决策时停止并请求人类确认

# 代码风格

- 函数/变量命名用完整英文单词，避免缩写
- 错误必须显式处理，禁止空 catch
- 新增公共 API 必须有文档注释
- 遵循现有文件的 import 顺序和命名风格

# 测试要求

- 新功能必须附带测试
- Bug 修复必须附带回归测试
- 不允许删除现有测试来让 CI 通过

# 安全

- 禁止提交 .env、credentials、token
- 禁止硬编码密钥
- 外网请求需经 Security Owner 审批的配置白名单
