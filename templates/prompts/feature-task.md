# Prompt 模板：新功能任务

复制以下模板到 Agent 会话，替换 `{{}}` 占位符。

---

## 任务

实现功能：{{功能名称}}

## 背景

{{为什么需要这个功能，链接到 issue/需求文档}}

## 验收标准

- [ ] {{标准 1}}
- [ ] {{标准 2}}
- [ ] {{标准 3}}

## 范围约束

- **允许修改**：{{目录/文件列表}}
- **禁止修改**：{{目录/文件列表}}
- **最大 diff**：400 行

## 回归范围

- {{需要手动或自动验证的现有功能}}

## 禁止事项

- 不做无关重构
- 不引入新依赖（除非列出并获得审批）
- 不修改 `.cursor/rules/` 和 CI 配置

## 执行步骤

1. 先做影响面分析（使用 `templates/prompts/impact-analysis.md`）
2. 输出实现计划，等待确认
3. 编码并运行 `./scripts/validate.sh`
4. 完成自审（使用 `templates/prompts/self-review.md`）
5. 提交 PR，使用 PR 模板
